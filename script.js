myLibrary = [];

const Book = function(title, author, pages, edition){
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.edition = edition;
}

function addBookToLibrary() {
    // take parameters, Create new book, add it to library
}